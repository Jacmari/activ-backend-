{
  "name": "activ-banking-backend",
  "version": "1.0.0",
  "private": true,
  "description": "ACTIV backend (Plaid + JAMARI) with Postgres token persistence for Heroku",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}